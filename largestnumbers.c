/*largest of any two  numbers */
#include<stdio.h>
 main()
{ 
int a,b;
printf("Enter a,b values\n");
scanf("%d%d",&a,&b);
if(a>b)
printf("a is big=%d\n",a);
else
printf("b is big=%d\n",b);
}

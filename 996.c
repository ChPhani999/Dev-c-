#include<stdio.h>
main()
{
	int a;
	printf("\n enter the number:");
	scanf("%d",&a );
	if (a%5,a%10==0)
	{
		printf("\n %d is divisible by 5,10.",a);
	}
	else 
	{
		printf("\n %d is not divisible by  5,10.",a);

	}
     
}
